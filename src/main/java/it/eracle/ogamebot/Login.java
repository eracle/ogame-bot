package it.eracle.ogamebot;

import java.net.URL;

/**
 * Represent the action of a Login to a specified server and with a specified username and password.
 * Once Logged an <code>OgameConnection</code> object instance is returned.
 *
 */
public class Login
{
    /**
     * Connects to the server with the username and password passed.
     * TODO: Finish the documentation after implementation phase. FDAIP
     * @param serverUrl
     * @param username
     * @param password
     * @return
     */
    public static OgameConnection login(String serverUrl,String username,String password)
    {
        return null;
    }
}
